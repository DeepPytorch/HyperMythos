# HyperMythos
This would be, if the rest of the code is added (Which I did not do for safety purposes, this is just the model architecture and tokenizer, no trainining, no testing.), and if it is trained would he the most powerful AI ever made. This model may, if fully trained, be in the realm of AI superintellegence, more powerful than Claude Mythos, and could significantly exceed human capabilities in a large number of fields.
# The Alignment Problem
This model could only be trained on a <u>specific</u> type of data, that may possibly fix the problem of alignment.
When a model of this size (17.6 Trillion parameters) is trained, it picks up on every single piece of bias in the data. The only way a model of this size could be trained, because of its immense power and size, while still keeping it aligned with human values is to both use a large neural network to monitor its thinking, and by engineering a bias in the data, to keep the model away from ideas such as escaping containment, and to keep it away from telling people illegal stuff such as how to make weapons.
# The Model
This model uses a looped transfomer MoE, a seperate network to act as a router, and a a AI designed to determine each token's complexity to allow the easier tokens to exit early, and the more difficult tokens to stay in the model for longer, allowing the model to "think" on it longer.
# Why It should Never be Trained Without Extreme Safety Features
IF THIS MODEL IS EVER TRAINED, IT WILL POSSIBLY BE, IN ALL PRACTICAL FORMS, NARROW AI SUPERINTELLGENCE. THIS MODEL IS EXTREMELY DANGEROUS BECAUSE OF THIS, AND THUS SHOULD NEVER BE TRAINED WITH OUT THE MOST EXTREME AND MAXIMUM SAFETY FEATURES.
(This AI is based off Mythos, just extremely powerful.)
